# Feature modules

Business rules, application services, schemas, queries, actions, and feature-owned UI live here. Feature code depends on server adapters through explicit interfaces and must not call provider SDKs directly from pages or components.
