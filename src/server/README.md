# Server adapters

Database, storage, authentication, and AI provider integration code lives here. These modules are server-only and expose application-facing adapters rather than provider-specific details.
